namespace MovieAuthApi.Core.Enums
{
    // Simple, local Role enum used by the application's User model
    public enum PaymentStatus
    {
        Pending,
        Completed,
        Failed,
        Refunded
    }
}
